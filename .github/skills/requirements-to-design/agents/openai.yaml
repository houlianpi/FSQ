interface:
  display_name: "Requirements To Design"
  short_description: "Optionally clarify project design"
  default_prompt: "Use $requirements-to-design to clarify this project change and produce a confirmed design document for later $spec-driven input."
policy:
  allow_implicit_invocation: false
