interface:
  display_name: "Project SPEC Audit"
  short_description: "Audit project diff and SPEC delta mode"
  default_prompt: "Use $spec-implementation-audit to audit the project implementation diff and independently validate its confirmed-update or no-delta mode against current SPEC.md files."
policy:
  allow_implicit_invocation: false
